import { StyleSheet } from "react-native";
export var styles = StyleSheet.create({
    titulo: {
        textAlign: "center",
        alignItems: "center",
        justifyContent: "center",
    },
});
